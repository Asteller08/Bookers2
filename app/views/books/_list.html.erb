    <h2>User info</h2>
    <section>
      <%= attachment_image_tag @book.user, :profile_image, size: "100x100", fallback: "no_image.jpg"%><br />
      <table class='table'>
        <tr></tr>
        <tr>
    	    <th>name</th>
    	    <th><%= @book.user.name %></th>
        </tr>
        <tr>
    	    <th>introduction</th>
    	   <tr>
    	     <th>introduction</th>
    	     <th><%= @book.user.introduction %></th>
        </tr>
        </tr>
        <th>
          <% if @book.user.id == @book.user.id %>
            <p><%= link_to '', edit_user_path(@book.user.id), class:"btn-prof glyphicon glyphicon-wrench" %></p>
          <% else %>
            <p><%= link_to '', user_path(current_user.id), class:"btn-prof glyphicon glyphicon-wrench" %></p>
          <% end %>
        </th>
      </table>
    </section>