<section class="sct-color-1 slice">
  <div class="container">
    <% if flash[:notice] %>
      <div class = "flash">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <div class="row">
      <div class="col-xs-3">
        <%= render 'users/list', books: @books %>
        <%= render 'books/listc', books: @books %>
      </div>
      <div class="col-xs-9">
        <h1>Book detail</h1>
        <table class="table table-striped">
          <thead>
            <tr>
              <th colspan="1">
              <th>Title</th>
              <th>Opinion</th>
              <% if @book.user.id == current_user.id %>
                <th colspan="2">
              <% end %>
            </tr>
          </thead>
          <tbody>
          <tr>
            <td>
              <p><%= attachment_image_tag @book.user, :profile_image, size: "50x50" ,fallback: "no_image.jpg"%></p>
               <p><%= link_to user_path(@book.user.id) do %>
                  <%= @book.user.name %>
                <% end %></p>
              </td>
            <td><%= @book.title %></td>
            <td><%= @book.body %></td>
            <% if @book.user == current_user %>
                <td><%= link_to 'Edit', edit_book_path(@book.id), {class: "btn-edit"} %></td>
                <td><div class = "btn-delete"><%= link_to 'Destroy', book_path(@book), method: :delete %></div></td>
            <% end %>
          </tr>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>
